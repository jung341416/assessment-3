//grab DOM elements
const shopping_btn = document.getElementById('shopping_cart');
const add_btn = document.getElementById('click_btn');
const item_name = document.getElementsByClassName('image_name');
const item_price = document.getElementsByClassName('image_price');
const display_name = document.getElementsByClassName('display_name');
const display_price = document.getElementsByClassName('display_price');
//when buttons clicked set the item and price it to local storage
// when shoppingcart is clicked add a href which will locate to new page
  //display the stored values from localstorage on new page

  windows.onload((event)=> {
    event.target = 
  })